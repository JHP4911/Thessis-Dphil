\newcommand{\f}{\frac}
\newcommand{\p}{\partial}
\newcommand{\bs}{\boldsymbol}
\newcommand{\dd}{\mathrm{d}}

\renewcommand{\thefootnote}{\fnsymbol{footnote}}

%Make sure the bullet points for the itemize environment explicitly come from
%the math environment so that my warnings are stopped.
\renewcommand\textbullet{\ensuremath{\bullet}}

%Set a new enumeration environment to count my TODOs
\newcounter{myenumi}
\setcounter{myenumi}{0}
\newenvironment{myenumerate}{\begin{enumerate} \setcounter{enumi}{\themyenumi}}{\setcounter{myenumi}{\theenumi}\end{enumerate}}

%Define a new colour
\definecolor{seabornblue}{RGB}{76,114,176}

%Change \cite commands to \citep
\let\cite\citep

%force right justification of bibliography
\renewcommand*{\bibfont}{\raggedright}

%Style labels used in abbreviations page
\newcommand{\abbrlabel}[1]{\makebox[3cm][l]{\textbf{#1}\ \dotfill}}
